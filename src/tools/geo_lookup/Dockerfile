# syntax=docker/dockerfile:1.7
# geo_lookup - IP-to-geography lookup tool (used by fraud-detector).
# Thin layer on top of the shared tool-base image.

ARG TOOL_BASE=ghcr.io/stephenwagner-grafana/observibelity-tool-base:latest
FROM ${TOOL_BASE}

LABEL org.opencontainers.image.title="observibelity-geo_lookup" \
      org.opencontainers.image.description="IP-to-geography lookup tool" \
      org.opencontainers.image.source="https://github.com/stephenwagner-grafana/observibelity" \
      org.opencontainers.image.licenses="Apache-2.0"

ENV OTEL_SERVICE_NAME=geo_lookup \
    TOOL_NAME=geo_lookup

COPY --chown=app:app app/ /app/app/

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]

{{- if (and .Values.supportbot.enabled (le 2 (int .Values.phase))) }}
apiVersion: v1
kind: Service
metadata:
  name: supportbot
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "observibelity.labels" . | nindent 4 }}
    app.kubernetes.io/component: supportbot
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  selector:
    {{- include "observibelity.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: supportbot
{{- end }}

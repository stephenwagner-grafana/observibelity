{{- if (and .Values.k6.enabled (le 2 (int .Values.phase))) }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: k6-traffic
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "observibelity.labels" . | nindent 4 }}
    app.kubernetes.io/component: traffic
spec:
  replicas: {{ .Values.k6.replicas | default 1 }}
  selector:
    matchLabels:
      {{- include "observibelity.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: traffic
  template:
    metadata:
      labels:
        {{- include "observibelity.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: traffic
      annotations:
        checksum/scenarios: {{ include (print $.Template.BasePath "/k6/configmap.yaml") . | sha256sum }}
    spec:
      {{- $podSc := include "observibelity.podSecurityContext" .Values.k6.securityContext }}
      {{- if $podSc }}
      securityContext:
        {{- $podSc | nindent 8 }}
      {{- end }}
      containers:
        - name: k6
          image: {{ .Values.k6.image | default "grafana/k6:0.50.0" }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          command: ["sh", "-c"]
          args:
            - |
              set -e
              echo "ObserVIBElity k6 traffic engine starting"
              CYCLE_SECONDS="${CYCLE_SECONDS:-{{ .Values.k6.cycle_seconds | default 10 }}}"
              while true; do
                for script in /etc/k6/scripts/*.js; do
                  if [ -f "$script" ]; then
                    echo "Running $(basename "$script")"
                    k6 run --quiet --no-summary "$script" || true
                  fi
                done
                sleep "$CYCLE_SECONDS"
              done
          env:
            - name: NEONCART_URL
              value: "http://neoncart"
            - name: SUPPORTBOT_URL
              value: "http://supportbot"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://otel-collector:4318"
            {{- with .Values.k6.output }}
            - name: K6_OUT
              value: {{ . | quote }}
            {{- end }}
            - name: CYCLE_SECONDS
              value: "{{ .Values.k6.cycle_seconds | default 10 }}"
            - name: DEFAULT_RATE
              value: "{{ .Values.k6.default_rate | default "5m" }}"
            - name: LOADGEN_CLAUDE_FRACTION
              value: "{{ .Values.k6.claudeFraction | default 0.02 }}"
            # Virtual users per scenario. baseline.js + per-usecase scenarios
            # all default to 60 if unset; bump here to ramp continuous load.
            - name: K6_VUS
              value: "{{ .Values.k6.vus | default 60 }}"
          volumeMounts:
            - name: scripts
              mountPath: /etc/k6/scripts
              readOnly: true
          resources:
            {{- toYaml .Values.k6.resources | nindent 12 }}
          {{- $ctrSc := include "observibelity.containerSecurityContext" .Values.k6.securityContext }}
          {{- if $ctrSc }}
          securityContext:
            {{- $ctrSc | nindent 12 }}
          {{- end }}
      volumes:
        - name: scripts
          configMap:
            name: k6-scenarios
{{- end }}
